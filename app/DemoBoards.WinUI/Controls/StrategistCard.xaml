<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="DemoBoards_WinUI.Controls.StrategistCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DemoBoards_WinUI.Controls"
    mc:Ignorable="d">

    <Border x:Name="ShellBorder" Padding="16" CornerRadius="14" BorderThickness="1">
        <Grid RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="3">
                    <TextBlock x:Name="TitleText" FontSize="17" FontWeight="SemiBold" TextWrapping="WrapWholeWords" />
                    <TextBlock x:Name="MetaText" Opacity="0.68" FontSize="12" TextWrapping="WrapWholeWords" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                    <Border x:Name="PathStateBadge" Visibility="Collapsed" Padding="8,3" CornerRadius="10">
                        <TextBlock x:Name="PathStateText" FontSize="12" FontWeight="SemiBold" />
                    </Border>
                    <Border x:Name="StatusBadge" Padding="8,3" CornerRadius="10">
                        <TextBlock x:Name="StatusText" FontSize="12" FontWeight="SemiBold" />
                    </Border>
                    <Button x:Name="RefreshButton" Content="Refresh" />
                </StackPanel>
            </Grid>

            <TextBlock x:Name="RefreshStatusText" Grid.Row="1" Opacity="0.72" TextWrapping="WrapWholeWords" />

            <Grid x:Name="CoreHost" Grid.Row="2" />
        </Grid>
    </Border>
</UserControl>
