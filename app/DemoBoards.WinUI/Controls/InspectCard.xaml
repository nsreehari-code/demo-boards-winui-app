<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="DemoBoards_WinUI.Controls.InspectCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DemoBoards_WinUI.Controls"
    mc:Ignorable="d">

    <Grid ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="12">
                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="10">
                        <StackPanel Spacing="4">
                            <TextBlock x:Name="CardTitleText" FontSize="20" FontWeight="SemiBold" TextWrapping="WrapWholeWords" />
                            <TextBlock x:Name="CardSubtitleText" Opacity="0.72" TextWrapping="WrapWholeWords" />
                        </StackPanel>

                        <StackPanel x:Name="SummaryHost" Spacing="6" />

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="RefreshButton" Grid.Column="0" Content="Refresh card" />
                            <Button x:Name="RunCycleButton" Grid.Column="1" Content="Run full card preflight" />
                        </Grid>
                        <Button x:Name="DeleteButton" Content="Delete card" HorizontalAlignment="Stretch" />
                        <TextBlock x:Name="ActionStatusText" TextWrapping="WrapWholeWords" Opacity="0.8" />
                    </StackPanel>
                </Border>

                <controls:CardShell x:Name="PreviewShell" />

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Sources" FontSize="18" FontWeight="SemiBold" />
                        <StackPanel x:Name="SourcesHost" Spacing="8" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Files" FontSize="18" FontWeight="SemiBold" />
                        <StackPanel x:Name="FilesHost" Spacing="8" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Trial Run Output" FontSize="18" FontWeight="SemiBold" />
                        <StackPanel x:Name="FlightSummaryHost" Orientation="Horizontal" Spacing="8" />
                        <TextBlock x:Name="FlightStatusText" Opacity="0.72" TextWrapping="WrapWholeWords" />
                        <StackPanel x:Name="FlightSectionsHost" Spacing="8" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="14">
                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Backface" FontSize="18" FontWeight="SemiBold" />
                        <controls:CardBackface x:Name="BackfaceView" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Chat" FontSize="18" FontWeight="SemiBold" />
                        <controls:ChatPane x:Name="ChatPaneView" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Metadata" FontSize="18" FontWeight="SemiBold" />
                        <StackPanel x:Name="MetadataHost" Spacing="6" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Card Data" FontSize="18" FontWeight="SemiBold" />
                        <StackPanel x:Name="FieldsHost" Spacing="6" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Computed Values" FontSize="18" FontWeight="SemiBold" />
                        <StackPanel x:Name="ComputedHost" Spacing="6" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Token Flows" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="Requires" FontWeight="SemiBold" Opacity="0.8" />
                        <StackPanel x:Name="RequiresHost" Spacing="6" />
                        <TextBlock Text="Provides" FontWeight="SemiBold" Opacity="0.8" Margin="0,6,0,0" />
                        <StackPanel x:Name="ProvidesHost" Spacing="6" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Raw Definition" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock x:Name="DefinitionText" TextWrapping="WrapWholeWords" IsTextSelectionEnabled="True" FontFamily="Consolas" />
                    </StackPanel>
                </Border>

                <Border Padding="14" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Raw Runtime" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock x:Name="RuntimeText" TextWrapping="WrapWholeWords" IsTextSelectionEnabled="True" FontFamily="Consolas" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
