<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DemoBoards_WinUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DemoBoards_WinUI.Controls"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="28" RowSpacing="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Spacing="6">
                <TextBlock Text="Embedded Runtime Host" FontSize="32" FontWeight="SemiBold" />
                <TextBlock Text="The WinUI shell now owns V8 lifecycle, localfs storage selection, the Copilot/Foundry invocation bridge, and the localhost agentface surface." TextWrapping="WrapWholeWords" />
            </StackPanel>

            <Border Grid.Row="1" Padding="18" CornerRadius="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Runtime State" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Name="RuntimeStatusText" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Padding="18" CornerRadius="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Paths" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Name="RootDirectoryText" TextWrapping="WrapWholeWords" />
                    <TextBlock x:Name="StorageDirectoryText" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Padding="18" CornerRadius="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Agentface" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Name="AgentfaceEndpointText" TextWrapping="WrapWholeWords" />
                    <TextBlock Text="Current surface: `/healthz`, `/mcp`, and `/mcp-raw` on localhost. This is the app-hosted replacement for the old external runtime socket surface." TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <Border Grid.Row="4" Padding="18" CornerRadius="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Copilot / Foundry Invocation" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Name="InvocationText" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <Border Grid.Row="5" Padding="18" CornerRadius="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock Text="Board Snapshot" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                            <Button x:Name="AddCardButton" Content="Add card" Click="OnAddCardClick" />
                            <Button x:Name="RefreshButton" Content="Refresh" Click="OnRefreshClick" />
                        </StackPanel>
                    </Grid>
                    <controls:BoardCanvas x:Name="BoardCanvasView" MinHeight="280" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
